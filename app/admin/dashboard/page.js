export default function Dashboard() {
    return (
      <div>
        <h1 className="text-2xl font-bold">Admin Dashboard</h1>
        <p>Welcome to the admin panel. Overview of stats here.</p>
        {/* Add charts, stats, etc. */}
      </div>
    );
  }